<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .text-center {
            text-align: center;
        }

        .formlableGroup {
            position: relative;
        }

        .formlableGroup input[type="text"],
        input[type="email"],
        input[type="number"],
        input[type="password"] {
            outline: none;
            border: none;
            padding: 10px;
            border-radius: 7px;
            border: 1px solid gray;
            width: 260px;
        }

        .formlableGroup label {
            display: none;
            margin-bottom: 10px;
            padding-left: 5px;
            font-weight: 100;
            color: gray;
            position: absolute;
            top: 20%;
            transition: all 0.5s;
            pointer-events: none;
            font-weight: 500;
        }

        .formlableGroup input[type="text"]:focus,
        .formlableGroup input[type="email"]:focus,
        .formlableGroup input[type="password"]:focus,
        .formlableGroup input[type="number"]:focus {
            border-color: #333;
        }


        .formlableGroup input[type="text"]:focus+label,
        .formlableGroup input[type="email"]:focus+label,
        .formlableGroup input[type="password"]:focus+label,
        .formlableGroup input[type="number"]:focus+label {
            top: -8px;
            font-size: 12px;
            color: #333;
            z-index: 2;
            background-color: #fff;
            margin-left: 10px;
            padding: 0 5px;
            font-weight: normal;
            transition: all 0.5s;
            display: block;
        }


        #signup {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .formlableGroup {
            margin: 15px 0;
        }

        .cls-button {
            padding: 10px;
            border: none;
            background-color: #333;
            color: #fff;
            border-radius: 8px;
        }


        .formlableGroup input:focus::placeholder {
            color: transparent;
        }
    </style>
</head>

<body>
    <h1 class="text-center">Signup</h1>
    <% if(locals.error){ %>
    <div style="background-color: chocolate;padding: 20;border-radius: 10;text-align: center;margin: 0 20%;"><%=error%></div>
    <% } %>
    <div id="signup">
        <form action="/user/login" method="post">
            <div class="formlableGroup">
                <input type="email" name="email" id="email" placeholder="email">
                <label for="email">Email</label>
            </div>

            <div class="formlableGroup">
                <input type="password" name="password" id="password" placeholder="password">
                <label for="password">Password</label>
            </div>

            <button class="cls-button" type="submit">Signin</button>
        </form>
    </div>
</body>

</html>